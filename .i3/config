# vim:set foldmethod=marker foldmarker=(((,))) foldtext=foldtext() : 
# 
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# appearance (((
set $base00 #101218
set $base01 #1f222d
set $base02 #252936
set $base03 #7780a1
set $base04 #C0C5CE
set $base05 #d1d4e0
set $base06 #C9CCDB
set $base07 #ffffff
set $base08 #ee829f
set $base09 #f99170
set $base0A #ffefcc
set $base0B #a5ffe1
set $base0C #97e0ff
set $base0D #97bbf7
set $base0E #c0b7f9
set $base0F #fcc09e

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
font pango:System San Francisco Display 12

# Widow Colours
#                       border  background text    indicator
client.focused          $base01 $base01    $base07 $base0F
client.focused_inactive $base02 $base02    $base03 $base0F
client.unfocused        $base02 $base02    $base03 $base0F
client.urgent           $base02 $base08    $base00 $base0F

# I don't need i3wm to show where the next split is going to be
hide_edge_borders both

# I want to leave fullscreen when there is a popup
popup_during_fullscreen leave_fullscreen
#)))
# workspaces (((

workspace_layout tabbed

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

bindsym $mod+bracketright move workspace to output right
bindsym $mod+bracketleft move workspace to output left

# assign [class="Google-chrome"] $workspace2
# assign [title="PDF-XChange"] $workspace3
# assign [class="Rhythmbox" instance="rhythmbox"] $workspace10
#)))
# application-specific settings (((
for_window [class="Insync"] floating enable border pixel 1
for_window [title="Unlock private key"] floating enable border pixel 1
for_window [class="XWrits"] floating
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="xfreerdp"] floating disable
#)))
# keybindings (((
# mousebindings (((
# The middle button over a titlebar kills the window
bindsym --release button2 kill

# The middle button and a modifer over any part of the window kills the window
bindsym --whole-window $mod+button2 kill

# The right button toggles floating
bindsym button3 floating toggle
bindsym $mod+button3 floating toggle

# The side buttons move the window around
bindsym button9 move left
bindsym button8 move right
#)))

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+Escape exec --no-startup-id j4-dmenu-desktop --dmenu="/usr/local/bin/dmenu -i -fn 'System San Francisco Display-12'"
# for the line above to work I had to compile the latest version of dmenu
bindsym $mod+Tab exec --no-startup-id /usr/local/bin/dmenu_run -i -fn 'System San Francisco Display-12'

# the hud-alike
bindsym $mod+x exec ~/.i3/hud-menu/hud-menu.py dmenu

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+slash split h

# split in vertical orientation
bindsym $mod+minus split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+t floating toggle

# change focus between tiling / floating windows
bindsym $mod+t focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# )))
# exit mode (((
set $exit_mode System (l)ock, l(o)gout, (s)uspend, or s(h)utdown

mode "$exit_mode" {
  bindsym l exec i3lock -c 1f222d, mode "default"
  bindsym o exec ~/.i3/exit_menu.sh, mode "default"
  # bindsym s exec i3lock -c 1f222d, exec systemctl suspend, mode "default"
  bindsym s exec systemctl suspend, mode "default"
  bindsym h exec ~/.i3/shutdown_menu.sh, mode "default"

  bindsym Escape mode "default"
  bindsym Return mode "default"
}
bindsym $mod+q mode "$exit_mode"

# )))
# resize window (you can also use the mouse for that) (((
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym h           resize shrink width 10 px or 10 ppt
  bindsym j           resize grow height 10 px or 10 ppt
  bindsym k           resize shrink height 10 px or 10 ppt
  bindsym l           resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"
# )))
# vim mode (((
mode "vim-delete" {
  bindsym --release w exec xdotool key Control+Delete; mode "vim"
  bindsym --release ";" exec xdotool key Control+Shift+Delete; mode "vim"
  bindsym --release b exec xdotool key Control+BackSpace; mode "vim"
  bindsym --release "," exec xdotool key Control+Shift+BackSpace; mode "vim"
  bindsym --release d exec xdotool key Home Control+Shift+Delete; mode "vim"
  bindsym Return mode "vim"
  bindsym Escape mode "vim"
}
mode "vim" {
  # arrow-key equivalents
  bindsym --release h exec xdotool key Left
  bindsym --release l exec xdotool key Right
  bindsym --release j exec xdotool key Down
  bindsym --release k exec xdotool key Up
  # word movement
  bindsym --release w exec xdotool key --clearmodifiers Ctrl+Right
  bindsym --release b exec xdotool key --clearmodifiers Ctrl+Left
  # line movement
  bindsym --release ";" exec xdotool key --clearmodifiers End
  bindsym --release "," exec xdotool key --clearmodifiers Home
  # manipulate text
  bindsym --release x exec xdotool key Delete
  bindsym --release Shift+x exec xdotool key BackSpace
  # longer movement (41 keypresses)
  set $down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down Down
  set $up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up Up
  bindsym --release e exec xdotool key $down
  bindsym --release u exec xdotool key $up
  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
  # submodes
  bindsym --release d mode "vim-delete"
}
bindsym $mod+space mode "vim"
#)))
# i3bar (((
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command i3status
# }

bar {
  status_command i3blocks -c ~/.i3/i3blocks.conf
  font pango: System San Francisco Display, FontAwesome 12
  position bottom
  tray_output primary
  # strip_workspace_numbers yes
  colors {
    separator  $base03
    background $base01
    statusline $base05
    #                  border  background text
    focused_workspace  $base01 $base01    $base07
    active_workspace   $base01 $base02    $base03
    inactive_workspace $base01 $base01    $base03
    urgent_workspace   $base01 $base01    $base08
  }
}
# )))
# media keybindings (((
# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -- +5% # increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -- -5% # decrease sound volume
bindsym XF86AudioMute        exec --no-startup-id pactl set-sink-mute   @DEFAULT_SINK@   toggle # mute sound
bindsym XF86AudioMicMute     exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle # mute sound

# Screen brightness controls
bindsym XF86MonBrightnessUp   exec xbacklight -inc 5 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 5 # decrease screen brightness
bindsym $mod+b                exec ~/bin/turns-screen-on-off.sh # [b]lanks and un[b]lanks screen

# Media player controls
bindsym XF86AudioPlay  exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext  exec playerctl next
bindsym XF86AudioPrev  exec playerctl previous
#)))
# screen rotation shortcuts (((
bindsym $mod+i exec ~/bin/inverted-landscape.sh
bindsym $mod+Up exec ~/bin/inverted-landscape.sh
bindsym $mod+Down exec ~/bin/inverted-landscape.sh
bindsym $mod+Right exec ~/bin/portrait-landscape.sh
bindsym $mod+Left exec ~/bin/portrait-leftlandscape.sh
#)))
# keyboard and mouse settings (((
# keyboard layout
exec setxkbmap -layout br -variant dvorak-altdeadkeys 
exec setxkbmap -option terminate:ctrl_alt_bksp \
               -option caps:swapescape \
               -option ctrl:swap_lalt_lctl \
               -option ctrl:menu_rctrl \
               -option ctrl:rctl_menu \
               -option altwin:prsc_rwin \
               -option lv3:ralt_alt \
               -option lv3:space_switch
exec_always "sleep 1 ; ~/bin/xcape.sh ; ~/.i3/stops-and-starts-xcape.py"

# mouse settings
exec xinput set-prop 'SynPS/2 Synaptics TouchPad' 'libinput Tapping Enabled' 1
exec xinput set-prop 'SynPS/2 Synaptics TouchPad' 'libinput Natural Scrolling Enabled' 1
#)))
# autostarted applications (((
exec /usr/lib/gnome-settings-daemon/gnome-settings-daemon
exec --no-startup-id compton -b --backend glx --vsync opengl
exec --no-startup-id dropbox start
exec --no-startup-id insync start
exec --no-startup-id nm-applet
exec --no-startup-id redshift
exec --no-startup-id diodon
exec --no-startup-id ~/.i3/hud-menu/hud-menu-service.py
exec xwrits typetime=60 breaktime=5 +mouse +breakclock after=5 +multiply
exec urxvttmux.sh
exec_always --no-startup-id "sleep 1 ; feh --bg-fill ~/Pictures/wallpaper-poly.jpg"
exec_always ~/.i3/autoname_workspaces.py

exec xbacklight -set 30 # default brightness
exec --no-startup-id killall notify-osd
exec --no-startup-id dunst
#)))
# application shortcuts
# bindsym $mod+c exec google-chrome-stable
bindsym $mod+c exec env MOZ_USE_XINPUT2=1 firefox
bindsym $mod+n exec nautilus
bindsym $mod+v exec ~/.vim-anywhere/bin/run
bindsym $mod+u exec unity-control-center
bindsym --release $mod+p exec gnome-screenshot --interactive
# bindsym --release $mod+Shift+p exec gnome-screenshot --area --clipboard
## couldn't ever get the line above to work.
